name: assignment2
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase - Use latest compatible versions
  firebase_core: ^3.15.2  # Updated
  firebase_auth: ^5.7.0  # Updated
  cloud_firestore: ^5.6.12  # Updated
  
  # State Management
  provider: ^6.1.2
  
  # Maps & Location - Using flutter_map (OpenStreetMap)
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  geolocator: ^13.0.3
  url_launcher: ^6.3.1
  
  # Image Picker
  image_picker: ^1.1.2  # Updated
  
  # Utils
  intl: ^0.20.2  # Updated
  
  # UI
  cupertino_icons: ^1.0.8


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0  # Updated

flutter:
  uses-material-design: true